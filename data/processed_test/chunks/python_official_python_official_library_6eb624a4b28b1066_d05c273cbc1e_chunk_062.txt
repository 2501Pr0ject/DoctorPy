tzinfo is tz: return self # Convert self to UTC, and attach the new timezone object. utc = (self - self. utcoffset()). replace(tzinfo=tz) # Convert from UTC to tz's local time. return tz. fromutc(utc) astimezone() tzinfo None None self. tzinfo. utcoffset(self) None timedelta tzinfo None None self. tzinfo. dst(self) None timedelta tzinfo None None self. tzinfo. tzname(self) None time. struct_time time. localtime() d. timetuple() time. struct_time((d. year, d. month, d. day, d. hour, d. minute, d. second, d. weekday(), yday, dst)) yday = d. toordinal() - date(d. year, 1, 1). toordinal() + 1 tm_isdst dst() tzinfo None dst() None tm_isdst -1 dst() tm_isdst tm_isdst datetime d d. timetuple() tm_isdst d. dst() d d d. utcoffset() time. struct_time tm_isdst OverflowError d. year MINYEAR MAXYEAR datetime datetime datetime. utctimetuple() datetime datetime. replace(tzinfo=timezone. utc) datetime. timetuple() self. date(). toordinal() datetime float time. time() datetime mktime() datetime mktime() OverflowError OSError datetime (dt - datetime(1970, 1, 1, tzinfo=timezone. utc)). total_seconds() timestamp() fold datetime tzinfo=timezone. utc timestamp = dt. replace(tzinfo=timezone. utc). timestamp() timestamp = (dt - datetime(1970, 1, 1)) / timedelta(seconds=1) self. date(). weekday() isoweekday() self. date(). isoweekday() weekday() isocalendar() year week weekday self. date(). isocalendar() YYYY-MM-DDTHH:MM:SS. ffffff microsecond YYYY-MM-DDTHH:MM:SS microsecond utcoffset() None YYYY-MM-DDTHH:MM:SS. ffffff+HH:MM[:SS[. ffffff]] microsecond YYYY-MM-DDTHH:MM:SS+HH:MM[:SS[.