rmdir() removedirs() os. removedirs('foo/bar/baz') 'foo/bar/baz' 'foo/bar' 'foo' OSError Raises an auditing event os. remove with arguments path, dir_fd. os. remove path dir_fd Changed in version 3. 6: Accepts a path-like object. Rename the file or directory src to dst. If dst exists, the operation will fail with an OSError subclass in a number of cases: OSError On Windows, if dst exists a FileExistsError is always raised. The operation may fail if src and dst are on different filesystems. Use shutil. move() to support moves to a different filesystem. FileExistsError shutil. move() On Unix, if src is a file and dst is a directory or vice-versa, an IsADirectoryError or a NotADirectoryError will be raised respectively. If both are directories and dst is empty, dst will be silently replaced. If dst is a non-empty directory, an OSError is raised. If both are files, dst will be replaced silently if the user has permission. The operation may fail on some Unix flavors if src and dst are on different filesystems. If successful, the renaming will be an atomic operation (this is a POSIX requirement). IsADirectoryError NotADirectoryError OSError This function can support specifying src_dir_fd and/or dst_dir_fd to supply paths relative to directory descriptors. If you want cross-platform overwriting of the destination, use replace(). replace() Raises an auditing event os. rename with arguments src, dst, src_dir_fd, dst_dir_fd. os. rename src dst src_dir_fd dst_dir_fd Changed in version 3. 3: Added the src_dir_fd and dst_dir_fd parameters. Changed in version 3. 6: Accepts a path-like object for src and dst. Recursive directory or file renaming function. Works like rename(), except creation of any intermediate directories needed to make the new pathname good is attempted first. After the rename, directories corresponding to rightmost path segments of the old name will be pruned away using removedirs(). rename() removedirs() Note This function can fail with the new directory structure made if you lack permissions needed to remove the leaf directory or file. Raises an auditing event os. rename with arguments src, dst, src_dir_fd, dst_dir_fd. os. rename src dst src_dir_fd dst_dir_fd Changed in version 3.