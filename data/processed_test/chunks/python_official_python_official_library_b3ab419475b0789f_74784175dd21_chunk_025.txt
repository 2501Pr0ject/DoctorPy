/my_program") Path("my_program") PATH As a consequence of these differences, pathlib is not a drop-in replacement for os. path. os. path ### Corresponding tools¶ Below is a table mapping various os functions to their corresponding PurePath/Path equivalent. os PurePath Path os and os. path os os. path pathlib pathlib os. path. dirname() os. path. dirname() PurePath. parent PurePath. parent os. path. basename() os. path. basename() PurePath. name PurePath. name os. path. splitext() os. path. splitext() PurePath. stem, PurePath. suffix PurePath. stem PurePath. suffix os. path. join() os. path. join() PurePath. joinpath() PurePath. joinpath() os. path. isabs() os. path. isabs() PurePath. is_absolute() PurePath. is_absolute() os. path. relpath() os. path. relpath() PurePath. relative_to() [1] PurePath. relative_to() os. path. expanduser() os. path. expanduser() Path. expanduser() [2] Path. expanduser() os. path. realpath() os. path. realpath() Path. resolve() Path. resolve() os. path. abspath() os. path. abspath() Path. absolute() [3] Path. absolute() os. path. exists() os. path. exists() Path. exists() Path. exists() os. path. isfile() os. path. isfile() Path. is_file() Path. is_file() os. path. isdir() os. path. isdir() Path. is_dir() Path. is_dir() os. path. islink() os. path. islink() Path. is_symlink() Path. is_symlink() os. path. isjunction() os. path. isjunction() Path. is_junction() Path. is_junction() os. path. ismount() os. path. ismount() Path. is_mount() Path. is_mount() os. path. samefile() os. path. samefile() Path. samefile() Path. samefile() os. getcwd() os. getcwd() Path. cwd() Path. cwd() os. stat() os. stat() Path. stat() Path. stat() os. lstat() os.