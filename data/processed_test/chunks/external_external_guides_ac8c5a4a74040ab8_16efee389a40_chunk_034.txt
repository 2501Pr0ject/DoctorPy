* > > '<' '<html>' '>' '</title>' *. +. {m,n}. '>' '<' '>' >>> print(re. match('<. *. >', s). group()) <html> re. VERBOSE re. VERBOSE dog | cat dog|cat [a b] 'a' 'b' # pat = re. compile(r""" \s* # Skip leading whitespace (. P<header>[^:]+) # Header name \s* : # Whitespace, and a colon (. P<value>. *. ) # The header's value -- *. used to # lose the following trailing whitespace \s*$ # Trailing whitespace to end-of-line """, re. VERBOSE) pat = re. compile(r"\s*(. P<header>[^:]+)\s*:(. P<value>. *. )\s*$") regex.